# 本例的产物都能由脚本重跑出来, 只保留脚本 / SUMO 输入 / 文档用的总览图.
# (git 支持逐目录的 .gitignore, 规则相对本目录生效)

# 渲染结果 (render_panda3d.py / render_blender.py / show_depth_result.py)
_outputs/
# Blender 离线渲染的中间产物 (剧集 JSON + 缓存签名)
_blender/
# 静态场景产物: build_static_scene.py 一次生成 (*.glb 已由仓库根 .gitignore 忽略)
# (scene.blend* 连 Blender 自动备份的 scene.blend1 一起忽略)
3d_assets/scene.blend*
3d_assets/scene.json
# tshub set_logger 按时间戳建的日志目录
20*_*/
*.log
__pycache__/

# 例外: 文档里引用的总览图要跟着仓库走
!showcase.png
